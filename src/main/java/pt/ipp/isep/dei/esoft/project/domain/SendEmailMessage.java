package pt.ipp.isep.dei.esoft.project.domain;

/**
 * The interface Send email message.
 */
public interface SendEmailMessage{
    /**
     * Export to file.
     *
     * @param order the order
     */
    void exportToFile(Order order);
}
