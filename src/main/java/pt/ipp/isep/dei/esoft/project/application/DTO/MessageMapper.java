package pt.ipp.isep.dei.esoft.project.application.DTO;

import pt.ipp.isep.dei.esoft.project.domain.Message;

import java.time.LocalDate;

/**public class MessageMapper {
    MessageDTO toDto(Message domain){
        MessageDTO dto= new MessageDTO(domain.getDate(), domain.getSchedule(), domain.getName(), domain.getPhone(), domain.getPhone(), domain.getAgent());
        return dto;
    }
}*/
